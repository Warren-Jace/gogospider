# GogoSpider v3.3 快速迁移指�?

## 📌 核心变更

### �?已完成的9项优�?

1. **批量扫描和URL二选一** - 逻辑更清�?
2. **Cookie配置简�?* - 统一在配置文件中
3. **命令行参数精简** - 复杂配置移至配置文件
4. **配置默认值优�?* - 开箱即�?
5. **HTTPS证书忽略** - 支持自签名证�?
6. **JS文件正确处理** - 始终访问和分�?
7. **静态资源优�?* - 记录但不请求
8. **范围外URL记录** - 不丢失任何发�?
9. **CDN JS智能拼接** - 自动处理相对URL

## 🔄 需要迁移的配置

### 1. Cookie配置 (必须迁移)

**�?旧方�?- 已废�?*:
```bash
spider -url https://example.com -cookie-file cookies.json
spider -url https://example.com -cookie "session=xxx"
```

**�?新方�?- 配置文件**:
```json
{
  "target_url": "https://example.com",
  "anti_detection_settings": {
    "cookie_file": "cookies.json",
    "cookie_string": ""
  }
}
```

### 2. JS文件排除 (自动修复)

**�?旧配�?- 会导致JS文件被跳�?*:
```json
{
  "scope_settings": {
    "exclude_extensions": ["jpg", "png", "css", "js"]
  }
}
```

**�?新配�?- JS会被特殊处理**:
```json
{
  "scope_settings": {
    "exclude_extensions": ["jpg", "png", "css"]
    // �?移除"js",程序会自动访问JS文件
  }
}
```

**注意**: 即使配置中包�?js",程序也会自动访问JS文件,无需手动修改

### 3. 静态资源配�?(推荐优化)

**�?推荐配置** - 只记录不请求,提升70%效率:
```json
{
  "scope_settings": {
    "exclude_extensions": [
      "jpg", "jpeg", "png", "gif", "svg", "ico",
      "css", "woff", "woff2", "ttf",
      "mp4", "mp3", "pdf", "zip"
    ]
  }
}
```

### 4. HTTPS证书 (新增功能)

**场景**: 内网、自签名证书、证书过�?
```json
{
  "anti_detection_settings": {
    "insecure_skip_verify": true
  }
}
```

## 📝 快速检查清�?

### 检查项1: Cookie配置
- [ ] 是否使用�?`-cookie-file` �?`-cookie` 参数?
- [ ] 如果�?迁移到配置文件的 `anti_detection_settings`

### 检查项2: JS文件访问
- [ ] 配置文件中是否排除了js扩展�?
- [ ] 如果�?可以移除(程序会自动处�?,或保�?不影�?

### 检查项3: 批量扫描
- [ ] 是否使用 `-batch-file` 参数?
- [ ] 如果�?不需要再指定 `-url` 参数

### 检查项4: HTTPS站点
- [ ] 是否遇到证书错误?
- [ ] 如果�?设置 `insecure_skip_verify: true`

## 🚀 推荐配置模板

### 标准爬取模板
```json
{
  "target_url": "https://example.com",
  "depth_settings": {
    "max_depth": 3
  },
  "anti_detection_settings": {
    "request_delay": 500000000,
    "insecure_skip_verify": false
  },
  "scope_settings": {
    "enabled": true,
    "exclude_extensions": [
      "jpg", "png", "gif", "css", "pdf", "zip"
    ]
  },
  "sensitive_detection_settings": {
    "enabled": true,
    "rules_file": "./sensitive_rules_standard.json"
  }
}
```

### 需要登录的站点
```json
{
  "target_url": "https://example.com",
  "anti_detection_settings": {
    "cookie_file": "cookies.json",
    "insecure_skip_verify": false
  }
}
```

### API接口发现
```json
{
  "target_url": "https://api.example.com",
  "scope_settings": {
    "include_paths": ["/api/*", "/v1/*", "/graphql"]
  }
}
```

### 内网自签名证书站�?
```json
{
  "target_url": "https://internal.company.com",
  "anti_detection_settings": {
    "cookie_file": "auth_cookies.json",
    "insecure_skip_verify": true
  }
}
```

## ⚠️ 常见问题

### Q1: 为什么JS文件没有被分�?
**A**: 检查配置文件中�?`exclude_extensions` 是否包含"js"。虽然程序会自动处理,但建议移除以避免混淆�?

### Q2: Cookie配置不生�?
**A**: 
1. 确保Cookie文件格式正确(JSON或文本格�?
2. 检查配置路径是否正�?
3. 查看日志输出,确认Cookie是否加载成功

### Q3: 静态资源还是被请求?
**A**: 确保 `scope_settings.enabled: true` 且正确配置了 `exclude_extensions`

### Q4: HTTPS证书错误?
**A**: 设置 `anti_detection_settings.insecure_skip_verify: true`

### Q5: CDN JS文件中的URL没有被提�?
**A**: 
1. 确认JS文件确实来自CDN(程序支持60+ CDN)
2. 查看日志,确认JS文件是否被下载和分析
3. 相对URL会自动与目标域名拼接

## 📋 迁移步骤

### 步骤1: 备份旧配�?
```bash
cp my_config.json my_config.json.backup
```

### 步骤2: 更新配置文件
```bash
# 1. 复制新模�?
cp config.json my_config.json

# 2. 填入你的目标URL和其他配�?
# 编辑 my_config.json
```

### 步骤3: 测试新配�?
```bash
spider -config my_config.json
```

### 步骤4: 对比结果
```bash
# 检查输出文�?
ls spider_*_urls.txt
ls spider_*_sensitive.txt
```

## 💡 优化建议

### 提升效率
1. �?使用配置文件而不是命令行参数
2. �?排除不需要的静态资�?
3. �?合理设置深度和并�?
4. �?启用敏感信息检�?

### 提升覆盖�?
1. �?不要排除JS文件
2. �?启用JS分析
3. �?启用动态爬�?
4. �?配置合理的User-Agent

### 提升稳定�?
1. �?配置证书验证选项
2. �?使用Cookie认证
3. �?设置合理的请求延�?
4. �?启用速率限制

## 📞 支持

如有问题,请查�?
- [完整更新日志](CHANGELOG_v3.3.md)
- [配置指南](CONFIG_GUIDE.md)
- [示例配置](config.json)

---
**版本**: v3.3  
**更新日期**: 2025-10-26


