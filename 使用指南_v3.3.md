# GogoSpider v3.3 使用指南

## 🚀 快速开�?

### 最简单的使用
```bash
# 直接扫描一个URL
spider -url https://example.com
```

### 推荐使用方式（配置文件）
```bash
# 1. 复制示例配置
cp config.json my_config.json

# 2. 编辑配置文件，修改target_url为你的目�?
# 使用任何文本编辑器打开 my_config.json

# 3. 运行爬虫
spider -config my_config.json
```

## 📖 核心理念

### �?命令�?= 快速简�?
适合快速测试、一次性任�?
```bash
spider -url https://example.com -depth 3
```

### �?配置文件 = 完整强大
适合复杂场景、可复用配置
```json
{
  "target_url": "https://example.com",
  "depth_settings": { "max_depth": 5 },
  "anti_detection_settings": {
    "cookie_file": "cookies.json",
    "insecure_skip_verify": true
  }
}
```

### �?二者结�?= 灵活高效
配置文件 + 命令行参数覆�?
```bash
spider -config my_config.json -depth 10
```

## 🎯 使用场景

### 场景1: 快速扫描（无需配置�?
```bash
# 最简单的使用
spider -url https://example.com

# 设置深度
spider -url https://example.com -depth 5

# 使用代理
spider -url https://example.com -proxy http://127.0.0.1:8080
```

### 场景2: 需要Cookie认证
**方式1: 配置文件（推荐）**
```json
{
  "target_url": "https://example.com",
  "anti_detection_settings": {
    "cookie_file": "cookies.json"
  }
}
```
```bash
spider -config config_with_cookie.json
```

**cookies.json格式**:
```json
{
  "cookies": [
    {"name": "session", "value": "xxx", "domain": "example.com"},
    {"name": "token", "value": "yyy", "domain": "example.com"}
  ]
}
```

或使用Cookie字符�?
```json
{
  "anti_detection_settings": {
    "cookie_string": "session=xxx; token=yyy"
  }
}
```

### 场景3: 自签名证�?证书过期
```json
{
  "target_url": "https://internal.company.com",
  "anti_detection_settings": {
    "insecure_skip_verify": true
  }
}
```
```bash
spider -config config_insecure.json
```

### 场景4: API接口发现
```json
{
  "target_url": "https://api.example.com",
  "scope_settings": {
    "include_paths": ["/api/*", "/v1/*", "/graphql"],
    "exclude_extensions": ["jpg", "png", "css"]
  }
}
```

### 场景5: 批量扫描
**创建targets.txt**:
```
https://example1.com
https://example2.com
https://example3.com
```

**使用默认配置**:
```bash
spider -batch-file targets.txt
```

**使用配置文件**:
```bash
spider -batch-file targets.txt -config my_config.json
```

### 场景6: 敏感信息检�?
```json
{
  "target_url": "https://example.com",
  "sensitive_detection_settings": {
    "enabled": true,
    "rules_file": "./sensitive_rules_standard.json",
    "min_severity": "MEDIUM"
  }
}
```

### 场景7: 静态资源过滤（提升效率�?
```json
{
  "scope_settings": {
    "exclude_extensions": [
      "jpg", "png", "gif", "css",
      "woff", "ttf", "mp4", "pdf"
    ]
  }
}
```
**效果**: 减少70%+的HTTP请求，静态资源URL仍会记录

### 场景8: 黑名单配�?
```json
{
  "blacklist_settings": {
    "enabled": true,
    "domains": [
      "*.gov.cn",
      "*.edu.cn",
      "*.mil.cn"
    ],
    "strict_mode": true
  }
}
```
**效果**: 黑名单域名不访问，但URL会记�?

## 📂 配置文件详解

### 完整配置结构
```json
{
  "target_url": "https://example.com",
  
  "depth_settings": {
    "max_depth": 3,
    "deep_crawling": true
  },
  
  "anti_detection_settings": {
    "request_delay": 500000000,
    "cookie_file": "",
    "cookie_string": "",
    "insecure_skip_verify": false
  },
  
  "scope_settings": {
    "enabled": true,
    "exclude_extensions": ["jpg", "png", "css"]
  },
  
  "sensitive_detection_settings": {
    "enabled": true,
    "rules_file": "./sensitive_rules_standard.json"
  },
  
  "blacklist_settings": {
    "enabled": true,
    "domains": ["*.gov.cn"]
  }
}
```

### 关键配置项说�?

#### 1. Cookie配置
```json
"anti_detection_settings": {
  "cookie_file": "cookies.json",     // Cookie文件路径
  "cookie_string": "session=xxx"     // 或Cookie字符串（二选一�?
}
```

#### 2. HTTPS证书
```json
"anti_detection_settings": {
  "insecure_skip_verify": false    // true=忽略证书错误
}
```

#### 3. 静态资源过�?
```json
"scope_settings": {
  "exclude_extensions": [
    "jpg", "png", "gif", "svg", "ico",
    "css", "scss", "sass",
    "woff", "woff2", "ttf",
    "mp4", "mp3", "pdf", "zip"
  ]
}
```
**注意**: JS文件会自动访问，不需要从exclude_extensions中移�?

#### 4. 速率控制
```json
"rate_limit_settings": {
  "enabled": true,
  "requests_per_second": 50,
  "min_delay": 100,
  "max_delay": 1000
}
```

## 📊 输出文件说明

运行后会生成多个文件:

```
spider_example.com_20251026_120000_all_urls.txt          # 所有发现的URL
spider_example.com_20251026_120000_unique_urls.txt       # 去重后的URL
spider_example.com_20251026_120000_structure_unique.txt  # 结构化去重URL
spider_example.com_20251026_120000_params.txt            # 带参数的URL
spider_example.com_20251026_120000_apis.txt              # API端点
spider_example.com_20251026_120000_forms.txt             # 表单URL
spider_example.com_20251026_120000_sensitive.txt         # 敏感信息（文本）
spider_example.com_20251026_120000_sensitive.json        # 敏感信息（JSON�?
```

### URL文件区别

| 文件 | 说明 | 用�?|
|------|------|------|
| all_urls.txt | 所有发现的URL（包含静态资源） | 完整记录 |
| unique_urls.txt | 忽略参数值的去重URL | 减少重复 |
| structure_unique.txt | 结构化去重（识别路径变量�?| 最精简 |
| params.txt | 只包含带参数的URL | 参数测试 |
| apis.txt | 只包含API端点 | API测试 |

## 💡 最佳实�?

### 1. 准备不同场景的配置文�?
```bash
config_quick.json          # 快速扫�?
config_deep.json           # 深度扫描
config_api.json            # API发现
config_with_auth.json      # 需要认�?
config_insecure.json       # 忽略证书
```

### 2. 配置文件模板
将常用配置保存为模板，需要时复制修改:
```bash
cp config_template.json config_target1.json
# 修改target_url和相关配�?
```

### 3. 批量扫描最佳实�?
```bash
# 为批量扫描准备配�?
cat > batch_config.json << EOF
{
  "depth_settings": {"max_depth": 3},
  "anti_detection_settings": {
    "cookie_file": "common_cookies.json",
    "insecure_skip_verify": true
  }
}
EOF

# 批量扫描
spider -batch-file targets.txt -config batch_config.json
```

### 4. 静态资源处理建�?
```json
{
  "scope_settings": {
    "exclude_extensions": [
      "jpg", "jpeg", "png", "gif", "svg", "ico",
      "css", "scss", "sass",
      "woff", "woff2", "ttf", "eot",
      "mp4", "mp3", "avi", "pdf", "zip"
    ]
  }
}
```
**效果**: 
- 这些文件不会发送HTTP请求
- URL仍会记录到输出文�?
- JS文件自动访问（无需特殊配置�?
- 效率提升70%+

## �?常见问题

### Q1: Cookie怎么配置�?
**A**: 在配置文件中设置，不再支持命令行参数
```json
{
  "anti_detection_settings": {
    "cookie_file": "cookies.json"
  }
}
```

### Q2: 如何忽略HTTPS证书错误�?
**A**: 配置文件中设�?
```json
{
  "anti_detection_settings": {
    "insecure_skip_verify": true
  }
}
```

### Q3: JS文件没有被分析？
**A**: 不用担心，JS文件会自动访问和分析，即使在exclude_extensions中配置了js

### Q4: 静态资源还在被请求�?
**A**: 检查配�?
```json
{
  "scope_settings": {
    "enabled": true,
    "exclude_extensions": ["jpg", "png", "css"]
  }
}
```

### Q5: 批量扫描能用配置文件吗？
**A**: 可以！这是v3.3的新功能
```bash
spider -batch-file targets.txt -config my_config.json
```

### Q6: 命令行参数和配置文件冲突怎么办？
**A**: 命令行参数会覆盖配置文件
```bash
# 配置文件中depth=3，命令行depth=10，最终使�?0
spider -config my_config.json -depth 10
```

### Q7: 如何查看所有发现的URL（包括静态资源）�?
**A**: 查看 `*_all_urls.txt` 文件

### Q8: 超出范围的URL会丢失吗�?
**A**: 不会，超出范围的URL会记录到externalLinks，并保存到all_urls.txt

## 🔧 高级用法

### 组合使用
```bash
# 配置文件 + 命令行参�?+ 批量扫描
spider -batch-file targets.txt \
       -config my_config.json \
       -depth 10 \
       -proxy http://127.0.0.1:8080
```

### 管道模式
```bash
# 从其他工具导入URL
cat urls.txt | spider -stdin -config my_config.json
```

### 输出格式
```bash
# JSON输出
spider -config my_config.json -format json

# 只输出URL
spider -config my_config.json -format urls-only

# 静默模式
spider -config my_config.json -quiet
```

## 📚 相关文档

- **配置文件示例**: `config.json`
- **完整更新日志**: `CHANGELOG_v3.3.md`
- **快速迁移指�?*: `快速迁移指南_v3.3.md`
- **功能验证报告**: `功能验证报告_v3.3.md`
- **配置指南**: `CONFIG_GUIDE.md`

## 🎉 总结

### 推荐工作流程

1. **第一次使�?*
   ```bash
   spider -url https://example.com
   ```

2. **需要更多配�?*
   ```bash
   cp config.json my_config.json
   # 编辑配置
   spider -config my_config.json
   ```

3. **建立配置�?*
   ```bash
   config_quick.json       # 快速扫�?
   config_deep.json        # 深度扫描
   config_with_auth.json   # 需要认�?
   # 根据需要选择
   ```

4. **批量扫描**
   ```bash
   spider -batch-file targets.txt -config my_config.json
   ```

### 核心要点

�?**优先使用配置文件** - 更强大、更易维�? 
�?**命令行用于快速测�?* - 简单场�? 
�?**二者可以组�?* - 配置文件 + 命令行参数覆�? 
�?**为不同场景准备不同配�?* - 提高效率  

---

**版本**: v3.3  
**更新日期**: 2025-10-26  
**核心改进**: 配置简化、批量支持配置文件、帮助文档精简


